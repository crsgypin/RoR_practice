

<%= form_for @event, :url => { :controller => 'events', :action => 'delete', :id => @event} do |f| %>
	<%= simple_format(@event.name) %>
	<%= simple_format(@event.description) %>

	<%= simple_format("Are you sure to delete data?") %>

	<%= f.submit "delete" %>

<% end %>

